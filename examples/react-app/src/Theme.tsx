import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    mode: "dark",
    success: {
      main: "#01e676"
    },
    primary: {
      main: "#01e676"
    },
    background: {
      default: "#101111"
    },
  }
});

export default Theme;
